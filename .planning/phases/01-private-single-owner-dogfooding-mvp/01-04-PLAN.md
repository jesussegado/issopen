---
phase: 01-private-single-owner-dogfooding-mvp
plan: 04
type: execute
wave: 4
depends_on: [01-02, 01-03]
files_modified:
  - src/server/db/schema.ts
  - drizzle
  - src/server/domain/agents
  - src/server/mcp
  - src/web/routes/agents
  - src/web/routes/connect
  - tests/integration/mcp.test.ts
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, AGNT-04, AGNT-05, AGNT-06, MCP-01, MCP-02, MCP-03, MCP-06, MCP-07, MCP-10, MCP-11]
must_haves:
  truths:
    - "A named code agent receives a one-time revocable PAT stored only as a hash"
    - "Every agent call requires both an explicit scope and target project allowlist"
    - "ChatGPT can discover and authorize Remote MCP through OAuth 2.1 PKCE"
    - "The default Codex identity can return Ready for Review but cannot close"
  artifacts:
    - path: src/server/mcp
      provides: stateless MCP 2.0 tools shared with tracker services
    - path: src/web/routes/agents
      provides: one-time credential setup/revocation UI
    - path: src/web/routes/connect
      provides: ChatGPT instructions and OAuth consent UI
---
<objective>Add separately authenticated agent identities and the complete Remote MCP surface for ChatGPT OAuth and Codex PAT.</objective>
<context>@.planning/phases/01-private-single-owner-dogfooding-mvp/01-RESEARCH.md
@.planning/phases/01-private-single-owner-dogfooding-mvp/01-UI-SPEC.md
@.planning/phases/01-private-single-owner-dogfooding-mvp/01-02-PLAN.md</context>
<threat_model>
| Threat | Severity | Mitigation |
| --- | --- | --- |
| PAT disclosure | high | CSPRNG token, non-reversible hash, once-only response, redaction tests |
| Agent scope/project escalation | high | Server matrix + allowlist on every tool and negative cross-project suite |
| OAuth code interception/replay | high | PKCE S256, exact redirects, one-time short code, issuer/audience/expiry verification |
| Prompt injection changes operation | high | Fixed registered tools and schemas; issue text never enters dispatch/authorization |
| Agent closes without permission | high | Independent close scope enforced in domain transition and protocol test |
</threat_model>
<tasks>
<task type="auto"><name>Task 1: Implement agent credentials, scopes and allowlists</name><files>src/server/db/schema.ts, drizzle, src/server/domain/agents, src/server/http/agents.ts, src/web/routes/agents</files><read_first>.planning/REQUIREMENTS.md, .planning/phases/01-private-single-owner-dogfooding-mvp/01-UI-SPEC.md, src/server/domain</read_first><action>Add agent identity/project/scope/credential tables and owner APIs/UI. Generate prefixed high-entropy PATs, store only an Argon2id hash plus safe fingerprint, show once, update last-use, retain revoked attribution, and default Codex to all Phase 1 work scopes except close.</action><acceptance_criteria>Later reads never return a PAT; expiry/revocation is immediate; UI shows exact scopes/allowlist/last-use and requires confirmation.</acceptance_criteria><verify><automated>pnpm test:integration -- agents &amp;&amp; pnpm test -- secret</automated></verify></task>
<task type="auto"><name>Task 2: Add OAuth 2.1/CIMD and stateless MCP tools</name><files>src/server/auth.ts, src/server/mcp, src/server/app.ts, src/web/routes/connect, tests/integration/mcp.test.ts</files><read_first>src/server/auth.ts, src/server/domain, .planning/phases/01-private-single-owner-dogfooding-mvp/01-RESEARCH.md</read_first><action>Configure Better Auth MCP+CIMD with the configured canonical resource, PKCE, read/write scopes and compatibility DCR only as required by ChatGPT. Protect POST `/mcp` with OAuth verification or Issopen PAT resolution. Register bounded list/get/update/claim/release/link/move tools that pass authenticated actor context to the shared domain. Add consent/connect pages with exact requested scope copy.</action><acceptance_criteria>Discovery is standards-shaped; unauthenticated MCP returns an OAuth challenge; protocol tests prove scoped PAT/OAuth read/write and forbid cross-project/close; GET/DELETE `/mcp` are 405.</acceptance_criteria><verify><automated>pnpm test:integration -- mcp</automated></verify></task>
</tasks>
