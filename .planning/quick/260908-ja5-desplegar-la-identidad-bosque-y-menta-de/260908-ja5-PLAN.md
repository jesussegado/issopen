---
mode: quick
task: 260908-ja5
status: complete
---

# Desplegar la identidad aprobada de Issopen

Autorización explícita del propietario: «despliega», 2026-09-08.
Implementación inline con GSD quick, preservando fuente y GitOps separados.

1. Verificar fuente `2b0bb5a`, validaciones de `260907-wrj` y diferencias con
   la revisión productiva `f79e305`. Publicar la fuente en su origin privado,
   construir desde un archivo Git limpio, probar la imagen con PostgreSQL
   efímera y publicar un tag nuevo por digest real.
2. Preparar un worktree separado de `gitops/main`, cambiar únicamente la
   referencia de imagen/fuente de Issopen y registrar rollback. Validar
   Kustomize, repositorio y catálogo; publicar el commit GitOps autorizado.
3. Esperar la reconciliación automática y verificar Argo Synced/Healthy,
   digest efectivo, readiness, endpoints, PVC y PostgreSQL conservados,
   logs y favicon/paleta/logo realmente servidos por HTTPS. Registrar SHA,
   evidencia y estado GSD, sin mutaciones de datos de producto.

## Baseline y rollback

- Argo: `04ef9aa2c2733b4762c4d49a87cdb2a00135155c`, Synced/Healthy.
- Fuente anterior: `f79e305d7358f4d46a25f2ecfb13916f4824ea3a`.
- Imagen anterior:
  `registry.serviciosegado.com/issopen:epic-numbers-f79e305@sha256:61d2f3989b9fbbeccd48336c3534fa112334d9843641c0b6950f7d9d8e14b16d`.
- PostgreSQL: `issopen-postgres-0`, Ready, cero reinicios; PVC
  `data-issopen-postgres-0`, Bound, 5 GiB, local-path.
- Sin nuevas migraciones, dependencias o cambios de infraestructura. Rollback
  mediante revert del commit GitOps y reconciliación normal de Argo CD.
- El checkout principal de homelab está en una rama de trabajo distinta de
  main. El rollout se aislará sobre la revisión que consume Argo CD.

## Aceptación

Imagen construida desde fuente publicada, digest verificado en registry y pod;
Argo Synced/Healthy; HTTPS sirve logo y favicon exactos y tokens bosque/menta;
PostgreSQL/PVC no se recrean; commits relacionados y rollback documentados.
