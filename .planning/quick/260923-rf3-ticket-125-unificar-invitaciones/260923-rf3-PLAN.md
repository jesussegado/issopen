---
phase: quick
plan: 260923-rf3
type: execute
wave: 1
depends_on:
  - 260923-rf1
files_modified:
  - src/server/invitation-primitives.ts
  - src/server/invitations.ts
  - src/server/owner-invitations.ts
  - tests/unit/invitation-primitives.test.ts
  - docs/refactoring-v1.md
  - AGENTS.md
autonomous: true
requirements:
  - ISSOPEN-125
must_haves:
  truths:
    - "Member and Owner invitation flows share one implementation of their actual security primitives."
    - "Each flow keeps its own domain policies, transactions, errors and authentication endpoints."
    - "Token rotation, lifecycle precedence, email normalization and single-use redemption remain characterized."
    - "No HTTP, database or visible product contract changes."
---

<objective>
Remove repeated security primitives from member and Owner invitations without
creating a generic invitation service or changing either public flow.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Characterize the common primitives and invitation flows</name>
  <files>tests/unit/invitation-primitives.test.ts, tests/integration/http.test.ts, tests/integration/auth.test.ts</files>
  <action>Fix token format/hash, email normalization/masking, lifecycle precedence and the existing integration coverage for invalid, expired, used, rotated and concurrent redemption.</action>
  <verify>Run the focused unit, HTTP and auth integration suites.</verify>
  <done>The behavior to preserve is executable before delivery.</done>
</task>

<task type="auto">
  <name>Task 2: Extract only the real shared invitation primitives</name>
  <files>src/server/invitation-primitives.ts, src/server/invitations.ts, src/server/owner-invitations.ts</files>
  <action>Move common email, token, lifetime and lifecycle-state logic into a small pure module. Leave flow-specific policies, errors, transactions and endpoint wiring in their current services.</action>
  <verify>Search for removed local implementations; run lint, types, refactor gate and integration.</verify>
  <done>Both consumers use the same primitives while remaining distinct domain flows.</done>
</task>

<task type="auto">
  <name>Task 3: Publish and record production evidence</name>
  <files>docs/refactoring-v1.md, .planning/STATE.md</files>
  <action>Build an immutable image, update the isolated GitOps checkout and verify Argo, pod, public readiness and PVC identities before returning the issue to human review.</action>
  <verify>Exact digest runs Ready with zero restarts; public readiness and root pass.</verify>
  <done>The bounded refactor is deployed, documented and independently reviewable.</done>
</task>

</tasks>

<verification>
- focused primitive, HTTP and auth tests
- `pnpm refactor:check`
- `pnpm test`
- `pnpm test:integration`
- `pnpm build`
- `pnpm test:secrets`
- GitOps render and control-plane tests
</verification>

<success_criteria>
- Exactly one implementation of every primitive genuinely shared by both invitation flows.
- No generic service, schema change, new dependency or transport change.
- Complete local and production evidence is attached to ticket 125.
</success_criteria>
