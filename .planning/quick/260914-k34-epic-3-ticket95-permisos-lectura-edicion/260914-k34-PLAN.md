---
type: quick
status: complete
ticket: 95
epic: 3
source_base: 390f3d0
---

# 95 — Per-project read/edit permissions

Authority: user asks to continue all feasible Epic3 tickets; live95 question v2
selects role per project. Contract94 uses Owner/Member at workspace level and
read/edit at project level, never a global Viewer/Admin. Inline GSD execution.

## 1. Authoritative policy and additive migration

Store project membership permission, preserving existing grants as edit. One
central read/edit policy; no implicit access. Inventory every REST mutation,
capture/image operation, SSE and Chrome route. Own account/session operations
remain independent of project edit. Agent scopes/allowlists are unchanged.
Test real PostgreSQL: edit vs read vs absent grants, foreign workspace and UUIDs,
stale cookies/tokens after downgrade, protected attachments and SSE.

## 2. Effective capabilities in web and Chrome

Expose server-derived per-project permissions, hide denied mutation actions and
guard direct edit routes; read-only detail still shows answers/history. Chrome
only offers writable destinations, and server independently rejects old clients.
Keep existing Store0.6.2 compatible; do not alter its review submission.
Test read-only board/detail, editor behavior, direct endpoints and Chrome tokens.

## 3. Verify, release, continue

Record the matrix with OWASP Authorization Cheat Sheet reference, full suite and
negative tests. Back up/restore-check before migration, release immutable source
and GitOps refs, verify production with synthetic scoped smoke, update95 only
when verified. Continue96 member-management and remaining tickets; not a pause.
Future actions introduced by101–106 must reuse the same policy and repeat107.
