---
phase: quick
plan: 260923-rf1
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/refactoring-v1.md
  - scripts/check-schema-drift.ts
  - tests/unit/architecture-boundaries.test.ts
  - package.json
autonomous: true
requirements:
  - ISSOPEN-123
must_haves:
  truths:
    - "Every refactor ticket is linked to an evidenced hotspot, dependency order and non-regression boundary."
    - "Adapter boundaries fail automatically if web, Chrome, HTTP or MCP starts bypassing the domain with direct writes."
    - "Schema-only refactors have a reproducible drift check that cannot write into the repository."
    - "The critical authz, idempotency, audit, CAS, archive, live-refresh and Chrome-draft suites are mapped to runnable commands."
  artifacts:
    - path: "docs/refactoring-v1.md"
      provides: "Architecture map, hotspot inventory, execution order and contract matrix"
    - path: "tests/unit/architecture-boundaries.test.ts"
      provides: "Executable modular-monolith boundary characterization"
    - path: "scripts/check-schema-drift.ts"
      provides: "Temporary-copy Drizzle migration drift gate"
  key_links:
    - from: "package.json"
      to: "scripts/check-schema-drift.ts"
      via: "schema:check script"
    - from: "docs/refactoring-v1.md"
      to: "Epic 10 tickets 123-136"
      via: "hotspot and execution tables"
---

<objective>
Establish the safety net for Epic 10 before production code is reorganized: document the current architecture and duplication hotspots, make the intended execution order explicit, enforce the core adapter/domain boundaries, and provide a non-mutating schema drift check.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Record the architecture, hotspots and contract coverage</name>
  <files>docs/refactoring-v1.md</files>
  <action>Document the current module map, measured hotspots, concrete duplication, ticket mapping, dependency order, protected contracts and the exact focused/full validation commands. Keep evidence dated and distinguish existing characterization coverage from new guards.</action>
  <verify>Every ticket 123-136 appears once in the execution order and every critical contract named by ticket 123 maps to at least one existing test suite.</verify>
  <done>The refactor has one canonical, implementation-ready map without introducing a second product specification.</done>
</task>

<task type="auto">
  <name>Task 2: Add executable architecture and schema guards</name>
  <files>tests/unit/architecture-boundaries.test.ts, scripts/check-schema-drift.ts, package.json</files>
  <action>Add a source-import boundary test for the modular monolith and a schema check that copies Drizzle history to a temporary directory before running generate, then fails on any generated or modified migration artifact. Expose focused package scripts without changing runtime code.</action>
  <verify>pnpm test -- tests/unit/architecture-boundaries.test.ts; pnpm schema:check; pnpm lint; pnpm typecheck</verify>
  <done>The boundaries and zero-schema-drift promise are reproducible locally and in CI without mutating tracked migrations.</done>
</task>

<task type="auto">
  <name>Task 3: Run the characterization gate and close the tracker ticket</name>
  <files>.planning/quick/260923-rf1-ticket-123-red-seguridad-refactor/260923-rf1-SUMMARY.md, .planning/STATE.md</files>
  <action>Run the focused characterization command plus the normal fast suite, record exact results and commits, update project state, link the result in Issopen, move ticket 123 to the configured completion status, and release the claim.</action>
  <verify>git status is clean; the Issopen issue has evidence and no active claim.</verify>
  <done>Ticket 123 is independently reviewable and later refactor tickets can rely on its gates.</done>
</task>

</tasks>

<verification>
- `pnpm refactor:check`
- `pnpm test`
- `git diff --check`
- Review the generated documentation against live source sizes and test names.
</verification>

<success_criteria>
- No production behavior or public contract changes.
- Architecture and dependency boundaries are both documented and executable.
- Drizzle drift is checked in an isolated temporary directory.
- Ticket 123 contains the commit and validation evidence.
</success_criteria>
