---
phase: quick
plan: 260923-rf2
type: execute
wave: 1
depends_on:
  - 260923-rf1
files_modified:
  - src/server/db/schema.ts
  - src/server/db/schema/*.ts
  - tests/unit/architecture-boundaries.test.ts
  - docs/refactoring-v1.md
  - AGENTS.md
autonomous: true
requirements:
  - ISSOPEN-124
must_haves:
  truths:
    - "All existing schema exports remain available from src/server/db/schema.ts."
    - "The Drizzle generator sees exactly the same 42 tables and creates no migration artifact."
    - "Cohesive schema modules have an explicit acyclic dependency direction."
    - "Application consumers cannot couple to internal schema modules."
---

<objective>
Split the 1,681-line Drizzle schema into cohesive persistence modules while
preserving the public facade, SQL schema and runtime behavior exactly.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Separate cohesive table modules</name>
  <files>src/server/db/schema.ts, src/server/db/schema/*.ts</files>
  <action>Move declarations into identity, access, agents, tracker, notifications and captures modules. Keep cross-domain Drizzle relations in one documented integration module and retain schema.ts as the only public facade.</action>
  <verify>Compare every exported declaration before/after and run TypeScript.</verify>
  <done>All former exports resolve through the facade and no table module has a reverse dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Prove boundaries and zero SQL drift</name>
  <files>tests/unit/architecture-boundaries.test.ts</files>
  <action>Add guards against deep schema imports and forbidden module dependencies, then run the isolated Drizzle drift check and the complete integration suite.</action>
  <verify>pnpm schema:check; pnpm test:integration; pnpm refactor:check</verify>
  <done>Drizzle reports 42 unchanged tables, integration passes and the module graph is executable policy.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile architecture documentation and tracker evidence</name>
  <files>AGENTS.md, docs/refactoring-v1.md, .planning/STATE.md</files>
  <action>Document the stable facade and internal ownership rule, record validation and return ticket 124 to human review without an agent claim.</action>
  <verify>pnpm build; git status is clean; tracker contains commit and evidence.</verify>
  <done>The new schema structure is discoverable, verified and independently reviewable.</done>
</task>

</tasks>

<verification>
- exact before/after export-name comparison
- `pnpm schema:check`
- `pnpm refactor:check`
- `pnpm test`
- `pnpm test:integration`
- `pnpm build`
</verification>

<success_criteria>
- No public import changes outside the persistence implementation.
- No added, removed or modified migration artifact.
- All fast, focused, integration and build gates pass.
</success_criteria>
