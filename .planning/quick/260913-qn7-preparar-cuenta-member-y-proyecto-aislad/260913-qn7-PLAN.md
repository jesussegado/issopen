---
mode: quick
ticket: 86
status: complete
---

# Google review sandbox

User authorized a local Member review account on 2026-09-13. Canonical task:
86, version 5; question 29b94b55-a9de-4910-998a-e5b455468c49 v1 remains
unanswered in the UI; explicit approval is recorded in its MCP comment.

1. Add an operator-only, tested provisioning command: fresh local Member,
   one fresh synthetic project/Epic/sample tickets, transaction and audit,
   no existing-account adoption, no signup/auth/runtime changes. Generate the
   password into an exclusive 0600 ignored file, never stdout/Git. Removal
   reuses the existing membership revocation path. Keep access valid through
   review, then explicitly revoke; do not claim automatic account expiry.
2. Verify on isolated PostgreSQL: password login, exact allowlist, private
   project/issue denial, Owner-only denial, replay/collision safety and
   revocation. Provision via the authorized master after read-only preflight;
   validate production from a clean browser session with synthetic data only.
3. Save the credentials only in Google's private Test instructions and the
   protected operator file; document reviewer flow and rollback. Preserve the
   uploaded 0.6.2 package; no submission/publication claimed without observing
   it. Update ticket/STATE and commit source/docs, without a runtime rollout
   because only operator tooling is added.

GSD SDK is absent; initialized with the installed legacy gsd-tools.cjs
`init quick` entrypoint. Execute inline per the Codex skill adapter.
