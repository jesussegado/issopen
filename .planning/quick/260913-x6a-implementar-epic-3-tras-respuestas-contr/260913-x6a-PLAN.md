---
id: 260913-x6a
mode: quick
status: complete
---

# Epic 3 — reconcile decisions and deliver independent collaboration increments

Owner explicitly requested implementation after answering all 20 questions.
All questions were reread through MCP at v2; no unanswered warnings. Execute
inline via installed legacy GSD Quick initializer (gsd-sdk absent), no agents.
Source starts clean at f99ddd0; runtime and GitOps remain separate.

## 1. Contract 94

- Files: docs/epic-3-collaboration-plan.md, docs/workspace-memberships.md, AGENTS.md.
- Action: record selected branches and permissions matrix; define multiworkspace
  boundaries and create implementation ticket 108, distinct from contract 94.
  Retain Owner/Member at workspace level and read/edit permissions per project.
  Mark 100 (own global sessions) and 104 (current authorized detail) independent
  of the future tenancy migration; repeat integration against 95/108 later.
- Verify: all 20 answers against live versions; no inferred Admin, signup,
  automatic emails, extra identity grants or alteration of Store access.
- Done: contract linked to source and 94 Ready for Human Review, not Epic Done.

## 2. Ticket 100 — own web sessions

- Files: src/server/http/account.ts, app.ts, web AccountRoute/own-session
  component, integration/web/E2E tests, docs/account-sessions.md.
- Action: authenticated, no-store safe metadata and own-session revocation
  using Better Auth; list current/other sessions without returning tokens/IPs
  or raw User-Agent. Confirm one/others, handle failure/retry and own logout.
  Keep Chrome, Google provider and agents explicitly separate.
- Verify: PostgreSQL two sessions/two users, expiry, cross-user IDs, CSRF,
  immediate invalidation and no mutation of OAuth rows. Web keyboard/dialog,
  current-session distinction and retry; real isolated desktop/mobile E2E.
- Done: tests pass and evidence attached, no real sessions used as test targets.

## 3. Ticket 104 — authorized detail reconciliation

- Files: IssueDetailRoute, lib live reconciliation, question components if
  needed, web/E2E tests and docs.
- Action: reuse existing project events with focus/online/fallback refresh.
  Replace clean state only; preserve drafts, warn/compare explicitly when dirty;
  handle changed question IDs, 409 and access loss without exposing new data.
- Verify: multiple sessions, MCP invalidation, draft protection, retry, cleanup,
  revocation and regression of board/answers. No claim that future assignee or
  multiworkspace integration is already implemented.
- Done: verified source commits; full app/Chrome/Compose gates and scoped GitOps
  release with backup/restore and production read-only smoke before claiming
  deployment. If a gate fails, report it and retain the previous runtime.

No roadmap phase completion. Remaining approved work continues in live Epic 3;
do not treat these independent increments as completion of 95 or 108.

## Discovered prerequisite 109 — mobile navigation

Full E2E exposed an existing overlay left open after project navigation, hiding
board controls with larger project lists. Created 109 before fixing it.
Reset mobile menu on route/identity/workspace change, preserve Escape focus,
add unit tests and explicit mobile E2E assertion. Separate source commit;
same scoped runtime rollout. No forced clicks or hidden test-only workaround.
