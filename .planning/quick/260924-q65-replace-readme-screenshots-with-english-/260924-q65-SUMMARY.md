---
status: complete
quick_id: 260924-q65
date: 2026-09-24
---

# English README screenshots and tracked-tree audit

The two README images under `docs/assets/readme/` now match the already-reviewed English board and extension screenshots in `landing/assets/` byte for byte. README copy and image alt text were already English, so the image references did not need to change.

The audit found that this is only the first slice of the requested English-only GitHub tree. A scan of tracked Markdown, source, tests, HTML, JSON, and scripts found at least 155 files with Spanish diacritics or inverted punctuation: 72 under `.planning/`, 26 under `extensions/`, 18 under `skills/`, 16 under `docs/`, 10 under `tests/`, 9 under `src/`, 2 under `scripts/`, plus `AGENTS.md`, `PLATFORM.md`, and `init-project.md`. ASCII-only Spanish is additional. The checked-in Chrome Store screenshots and listing also contain Spanish. UI translation affects app and extension behavior/tests, so it must be coordinated with the owner's previous instruction that only the landing be English.

The public GitHub `main` observed through HTTPS is `0c7a99f`; the local source `main` is ahead. SSH access to the configured GitHub remote currently fails host-key verification. No GitHub push or product deployment was attempted. Concurrent authentication edits were not staged or modified.

Verification: visual review of both English source images; SHA-256 identity of each copied README image; tracked-file text audit; `git diff --check`.
